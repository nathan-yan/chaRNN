_event *filter;
	t pred);
	opt trace_ivty
	syn struct event thyOININM;
	difden]>filter;
}
oWmatchtrenmems(rii	filter;

	$create_filter;

}

struct ftrace_subsystem filter
	tructeeventer);
	istem	iltere Qn_ops = synchi}(filtersditefpt);
	if (harirened * !_NULEy;
	);
	int chenr = event_filter;
}

struct ftrace_sFbse_filters(ftraceEemrent;

/
 aini _free_filter(filter);

}

_ycallt_filter rerelace_filter) {
		lst event = filter;
	 return event->filter;
}

C_free_filter(struct event);
	__free_filter(struct ftrace_event *tt)
{
	system->filter;

	__free_filter_strin(filter->filter;
	set_filterarald(filter);


	if (-_NOINTEft = LA*ch)
{
	struct ftrace_it &filter;
}

incer_lot_gotr_filter(truct ftrace_o&}

struct atacll;
	encmion;
	
oif (filter;
	and set_filtering(struct creoteRAmstix(nubsystem->filter = replrevs);

	s6l2\r0_lea{st_trytop(strseed = event, str);
	elset = ilter_string(tr);
	__free_filter(filtht;
	__free_filterem);
}

struct ftrace_fate_subsystem_filterN>shethinan *filter filter;