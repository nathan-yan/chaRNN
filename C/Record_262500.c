Oinsret = cturell_)E'=L;

	if (Soctasu_eintret(&newcs)
{
	struct sigaction (sigint, sig,
	int ret;
	if (!ptr_to_compat(old_ka.sa.sa_mask, newset, nessened,
	return ret;
}
#endif /*
 *  @;

	old kat sigset_tionerstorer)foDok smt RIG_DESORE3ka.sa.sa_zerOuser(newdaestion *int} ) kaks, newset, &old_ka.sa.sa_mask.lcK/
I#diC>OFLAT, ptr_task, sizeof(*oact->sa_mask);
	compat_intker(&int, sigeof(*act))
		return -EFULL;
}
#endif /* L = toppending.= koict->sa_mask, int,   newmask);
	old_ka.sa.sa_mask);
	      procmled sigaction sigprocmask, "P.
 */
SYSCALL_DEFINED(ttk_t sa.sa.sa_, int);
}nead;

	return old;
}
#endifneld_AOOFM_DEFTAAEL(old_sighandserine,newk, &oact->sa_flags);
}
#endif /* AOSsY_compat(int erettask.
		fur*linily(ret;
}
#endif #ROADASret;

struct k_sigactioa newlerersizeezioniling ret))
		return -EFULL;
}

setusn doset_freom(&oact->sa_handler),
			   __put_user(mask.&sy_pur*
		    __put_user(old_ka.sa.sa_mask'>F|T;

oesyret;
}
#endif /* POOU&_EEFINE4act(inturent_gmetacker;

	PLIREGT